# =====================================================
# Global Test Configuration (applies to all microservices)
# =====================================================
spring:
  main:
    allow-bean-definition-overriding: true

  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop

  kafka:
    embedded:
      enabled: true
      partitions: 1
    consumer:
      group-id: test-group
      auto-offset-reset: earliest
      enable-auto-commit: false
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

  data:
    mongodb:
      port: 0
      database: test
      uri: mongodb://localhost:0/test

# -----------------------------------------------------
# Disable Eureka for tests
# -----------------------------------------------------
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

# -----------------------------------------------------
# Random ports to avoid conflicts
# -----------------------------------------------------
server:
  port: 0

# -----------------------------------------------------
# Logging (keep CI logs readable)
# -----------------------------------------------------
logging:
  level:
    root: INFO
    org.springframework.data.mongodb: WARN
    org.mongodb.driver: WARN
    org.apache.kafka: WARN

# =====================================================
# User Service Test Overrides
# =====================================================
user-service:
  spring:
    datasource:
      url: jdbc:h2:mem:userdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      driver-class-name: org.h2.Driver
      username: sa
      password:
    jpa:
      hibernate:
        ddl-auto: create-drop
    data:
      mongodb:
        database: user_service_test
        uri: mongodb://localhost:0/user_service_test

# =====================================================
# Product Service Test Overrides
# =====================================================
product-service:
  spring:
    datasource:
      url: jdbc:h2:mem:productdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      driver-class-name: org.h2.Driver
      username: sa
      password:
    jpa:
      hibernate:
        ddl-auto: create-drop
    data:
      mongodb:
        database: product_service_test
        uri: mongodb://localhost:0/product_service_test

# =====================================================
# Media Service Test Overrides
# =====================================================
media-service:
  spring:
    datasource:
      url: jdbc:h2:mem:mediadb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
      driver-class-name: org.h2.Driver
      username: sa
      password:
    jpa:
      hibernate:
        ddl-auto: create-drop
    data:
      mongodb:
        database: media_service_test
        uri: mongodb://localhost:0/media_service_test
